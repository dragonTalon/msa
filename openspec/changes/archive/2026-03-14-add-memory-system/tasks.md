# 实现任务：add-memory-system

## ⚠️ 风险警报

基于历史问题识别的风险：

### 高：敏感信息泄露

- **相关问题**：问题 003 - 不安全的配置
- **缓解措施**：实施内容过滤，检测并脱敏敏感信息
- **预防任务**：T-2.4 实现敏感信息过滤

### 中：启动性能退化

- **相关问题**：问题 004 - 浏览器超时配置
- **缓解措施**：使用索引加速查询，按需加载完整会话
- **预防任务**：T-1.3 实现内存索引

### 中：异步任务阻塞退出

- **相关问题**：问题 001 - AI 助手不应自动执行 git commit
- **缓解措施**：知识提取异步执行，不阻塞用户退出
- **预防任务**：T-2.3 实现异步知识提取

## 1. 核心存储层（第 1 周）

### 1.1 数据模型定义

- [x] 1.1.1 创建 `pkg/model/memory.go`

> 📚 **知识上下文**：遵循 Go 标准命名规范（编码规范）

**验收标准**：
- [x] 定义 `Session` 结构体（会话、上下文、消息）
- [x] 定义 `Knowledge` 结构体（6 种类型）
- [x] 定义 `ShortTermMemory` 结构体
- [x] 定义 `SessionIndex` 和 `KnowledgeIndex`（索引用）
- [x] 定义 `MemoryStats`（统计信息）
- [x] 所有 JSON tag 正确
- [x] 运行 `go vet ./pkg/model` 验证

### 1.2 存储接口和实现

- [x] 1.2.1 定义 `Store` 接口

> 📚 **知识上下文**：错误处理策略（pattern 003）- 区分可恢复和不可恢复错误

**验收标准**：
- [x] `SaveSession/GetSession/ListSessions/DeleteSession` 方法
- [x] `SaveKnowledge/GetKnowledge/ListKnowledgeByType/GetAllKnowledge` 方法
- [x] `SaveShortTermMemory/GetShortTermMemory` 方法
- [x] `Search` 方法（支持过滤）
- [x] `GetStats` 方法
- [x] 定义 `SearchFilters` 和 `SearchResult` 类型

- [x] 1.2.2 实现 `FileStore` 结构体

> 📚 **知识上下文**：单例模式（pattern 004）- 使用 sync.Once 确保只初始化一次

**验收标准**：
- [x] 实现 `NewFileStore()` - 创建目录结构
- [x] 实现 `loadIndexes()` - 加载索引，失败时自动重建
- [x] 实现 `saveIndexes()` - 持久化索引
- [x] `~/.msa/remember/` 目录结构创建成功

### 1.3 会话存储

- [x] 1.3.1 实现 `SaveSession()` 方法

> 📚 **知识上下文**：错误处理策略 - 文件写入失败时重试 2-3 次

**验收标准**：
- [x] 按年月组织目录（`sessions/2024-03/`）
- [x] JSON 格式保存完整会话
- [x] 更新 `SessionIndex`
- [x] 重试机制（临时错误）
- [ ] 测试：保存 1000 个会话，验证文件结构

- [x] 1.3.2 实现 `GetSession()` 和 `ListSessions()` 方法

**验收标准**：
- [x] `GetSession` 按时间查找文件
- [x] `ListSessions` 支持分页（offset, limit）
- [x] 按时间倒序排序
- [ ] 测试：创建 50 个会话，验证分页

### 1.4 知识存储

- [x] 1.4.1 实现 `SaveKnowledge()` 方法

**验收标准**：
- [x] 根据类型分目录保存
- [x] 更新 `KnowledgeIndex`
- [ ] 测试：保存各类型知识，验证文件结构

- [x] 1.4.2 实现 `GetAllKnowledge()` 方法

**验收标准**：
- [x] 按类型分组返回
- [ ] 测试：保存 100 个知识，验证分组

### 1.5 索引和搜索

- [x] 1.5.1 实现内存索引

> 📚 **知识上下文**：性能问题（pattern 004）- 避免全文件扫描

**验收标准**：
- [x] `Indexes` 结构体包含 `Sessions` 和 `Knowledge`
- [x] 支持增量更新（`insertOrUpdateIndex`）
- [ ] 测试：创建 1000 个会话，验证索引加载 < 200ms

- [x] 1.5.2 实现 `Search()` 方法

**验收标准**：
- [x] 关键词搜索（不区分大小写）
- [x] 股票代码搜索
- [x] 日期范围过滤
- [x] 类型过滤（session/knowledge）
- [x] 结果按相关性评分排序
- [ ] 测试：搜索 1000 个会话，响应 < 500ms

### 1.6 短期记忆

- [x] 1.6.1 实现 `SaveShortTermMemory()` 和 `GetShortTermMemory()` 方法

**验收标准**：
- [x] 保存到 `short-term/current.json`
- [x] 最多保存 10 条消息
- [ ] 测试：添加 20 条消息，验证只保留最新 10 条

### 1.7 统计功能

- [x] 1.7.1 实现 `GetStats()` 方法

**验收标准**：
- [x] 计算总会话数、总知识数
- [x] 找到最早/最新会话
- [x] 统计最常查询的股票（TOP 5）
- [x] 统计学得最多的概念（TOP 5）

## 2. 记忆管理器（第 2 周）

### 2.1 单例管理器

- [x] 2.1.1 实现 `Manager` 结构体和 `GetManager()`

> 📚 **知识上下文**：单例模式（pattern 004）- 使用 sync.Once 确保全局唯一实例

**验收标准**：
- [x] `Manager` 包含 `Store`、`knowledgeCache`、`currentSession`
- [x] `sync.Once` 确保只初始化一次
- [x] `GetManager()` 返回全局实例
- [ ] 测试：多次调用 `GetManager()`，验证是同一实例

- [x] 2.1.2 实现 `Initialize()` 方法

**验收标准**：
- [x] 生成 UUID 作为会话 ID
- [x] 初始化 `SessionManager`（开始时间、消息列表、上下文）
- [x] 初始化 `ShortTermMemory`（最多 10 条）
- [ ] 测试：调用 `Initialize()`，验证会话 ID 生成

### 2.2 消息记录

- [x] 2.2.1 实现 `AddMessage()` 方法

**验收标准**：
- [x] 添加到 `currentSession.messages`
- [x] 更新会话上下文（提取股票代码）
- [x] 更新短期记忆（保持最近 10 条）
- [x] 持久化 `short-term/current.json`
- [ ] 测试：添加 15 条消息，验证短期记忆只有 10 条

### 2.3 会话结束处理

- [x] 2.3.1 实现 `EndSession()` 方法

> 📚 **知识上下文**：AI 助手工作流程（guide 002）- 异步任务不阻塞用户

**验收标准**：
- [x] 构建 `Session` 对象（开始/结束时间、消息数）
- [x] 生成会话摘要（取第一条用户消息）
- [x] 提取标签（基于内容）
- [x] 同步保存完整会话
- [x] **异步**触发知识提取（不阻塞）
- [x] 清空 `currentSession`
- [ ] 测试：退出时立即返回，不等待知识提取

### 2.4 AI 知识提取

- [x] 2.4.1 实现 `ExtractKnowledgeWithAI()` 函数

> 📚 **知识上下文**：问题 001 - AI 助手不应自动执行影响性操作

**验收标准**：
- [x] 构建对话文本（过滤系统消息）
- [x] 构建知识提取 Prompt（详见设计文档）
- [x] 调用 LLM API（复用现有 agent）
- [x] 解析 JSON 响应
- [x] 返回 `[]Knowledge` 数组
- [ ] 测试：模拟对话，验证 JSON 解析

- [x] 2.4.2 实现 `extractKnowledgeAsync()` 方法

**验收标准**：
- [x] 在 goroutine 中执行
- [x] 失败时记录日志，不崩溃
- [x] 保存所有提取的知识
- [x] 刷新知识缓存
- [ ] 测试：退出后验证知识文件生成

### 2.5 敏感信息过滤

- [x] 2.5.1 实现 `redactSensitive()` 函数

> 📚 **知识上下文**：问题 003 - 不安全的配置，需注意敏感信息脱敏

**验收标准**：
- [x] 检测 API Key 模式（`sk-`、`Bearer `）
- [x] 检测密码模式（`password:`、`passwd:`）
- [x] 替换为 `***REDACTED***`
- [x] 在 `AddMessage()` 中调用
- [x] 在知识提取前调用
- [x] 测试：输入包含 API Key 的消息，验证被过滤

### 2.6 知识缓存

- [x] 2.6.1 实现 `LoadKnowledge()` 方法

**验收标准**：
- [x] 加载所有知识文件
- [x] 聚合用户画像（取最新的）
- [x] 合并关注列表（去重）
- [x] 收集所有概念（保留全部）
- [x] 更新 `knowledgeCache`
- [ ] 测试：创建 50 个知识，验证聚合正确

- [x] 2.6.2 实现 `GetKnowledgeForPrompt()` 方法

**验收标准**：
- [x] 返回 Markdown 格式的知识字符串
- [x] 包含用户画像、关注列表、学过的概念、常见问答
- [x] 控制长度（< 2000 tokens）
- [ ] 测试：生成知识字符串，验证格式正确

## 3. TUI 视图（第 3 周）

### 3.1 命令集成

- [x] 3.1.1 注册 `/remember` 命令

**验收标准**：
- [x] 实现 `RememberCommand` 结构体
- [x] 在 `init()` 中注册命令
- [x] 返回 Selector 视图
- [ ] 测试：输入 `/remember`，进入记忆主页

### 3.2 记忆主页

- [x] 3.2.1 实现 `MemoryHome` 视图

> 📚 **知识上下文**：参考 `model_selector.go` - 复用选择器组件和样式

**验收标准**：
- [x] 显示 4 个选项（会话、知识、搜索、统计）
- [x] 显示各选项数量
- [x] 支持 1-4 数字快捷键
- [x] 支持 `↑/↓` 导航
- [x] 支持 `Enter` 选择
- [x] 支持 `ESC`/`q` 返回聊天
- [ ] 测试：键盘导航所有选项

### 3.3 会话列表视图

- [x] 3.3.1 实现 `SessionList` 视图

**验收标准**：
- [x] 加载会话索引
- [x] 按时间倒序显示
- [x] 每个会话显示日期、消息数、摘要、标签、股票
- [x] 支持分页（10 条/页）
- [x] 支持 `↑/↓` 导航、`PgUp/PgDn` 翻页
- [x] 支持 `/` 搜索模式
- [x] 支持 `d` 删除
- [x] 支持 `Enter` 查看详情
- [x] 支持 `ESC` 返回
- [ ] 测试：创建 20 个会话，验证分页和搜索

### 3.4 会话详情视图

- [x] 3.4.1 实现 `SessionDetail` 视图

**验收标准**：
- [x] 加载完整会话 JSON
- [x] 显示元数据（时间、模型、股票、标签）
- [x] 显示完整对话（用户/助手消息）
- [x] 支持 `↑/↓` 滚动、`PgUp/PgDn` 翻页
- [x] 支持 `g` 跳到开头、`G` 跳到结尾
- [x] 支持 `/` 搜索（高亮匹配）
- [x] 支持 `e` 导出
- [x] 支持 `ESC` 返回
- [ ] 测试：加载 50 条消息的会话，验证滚动

### 3.5 知识列表视图

- [x] 3.5.1 实现 `KnowledgeList` 视图

**验收标准**：
- [x] 显示 6 个知识类型选择
- [x] 每个类型显示数量
- [x] 支持 1-6 数字快捷键切换类型
- [x] 显示该类型的所有知识
- [x] 按 `↑/↓` 导航
- [x] 按 `Enter` 查看详情
- [ ] 测试：切换所有类型，验证显示正确

### 3.6 搜索视图

- [x] 3.6.1 实现 `SearchView` 视图

**验收标准**：
- [x] 显示搜索输入框
- [x] 显示过滤器（类型、日期）
- [x] 实时显示搜索结果
- [x] 结果包含会话和知识
- [x] 按相关性排序
- [x] 显示匹配片段（高亮关键词）
- [x] 按 `Enter` 打开结果
- [ ] 测试：搜索关键词，验证结果正确

### 3.7 辅助函数

- [x] 3.7.1 实现通用辅助函数

**验收标准**：
- [x] `renderMemoryTitle()` - 渲染标题（已实现为 memoryTitleStyle）
- [x] `renderSearchBox()` - 渲染搜索框（已实现为 memorySearchBoxStyle）
- [x] `renderPositionInfo()` - 渲染位置信息（在 SessionList 中实现）
- [x] `renderEmptyResult()` - 渲染空结果（已实现为 memoryEmptyStyle）
- [x] `renderMemoryHelp()` - 渲染帮助信息（已实现为 memoryHelpStyle）
- [x] `highlightSnippet()` - 高亮搜索关键词（在搜索中实现）
- [x] `formatDuration()` - 格式化时长（使用 time.Format）

## 4. Chat 集成（第 3-4 周）

### 4.1 Chat 初始化

- [x] 4.1.1 修改 `NewChat()` 初始化记忆管理器

**验收标准**：
- [x] 添加 `memoryManager *memory.Manager` 字段
- [x] 调用 `memory.GetManager()` 获取单例
- [x] 调用 `memoryManager.Initialize()` 初始化会话
- [x] 测试：启动时验证会话 ID 生成

### 4.2 消息记录

- [x] 4.2.1 修改 `handleEnterKey()` 记录消息

**验收标准**：
- [x] 添加用户消息到 `history`（现有行为）
- [x] 同时调用 `memoryManager.AddMessage()`
- [x] 记录失败时不影响聊天
- [x] 测试：发送消息，验证记录成功

### 4.3 退出处理

- [x] 4.3.1 修改 `Update()` 处理 Ctrl+C/ESC

**验收标准**：
- [x] 按 Ctrl+C/ESC 时调用 `memoryManager.EndSession()`
- [x] 保存完成后退出
- [x] 测试：正常退出，验证会话文件生成

### 4.4 会话恢复功能（新增）

- [x] 4.4.1 退出时显示记忆ID

**验收标准**：
- [x] 退出时在终端显示会话ID
- [x] 提示用户可以使用 `--resume` 参数恢复会话
- [x] 格式：`会话已保存: {session-id}`
- [x] 提示：`提示: 使用 "msa --resume {session-id}" 恢复此会话`

- [x] 4.4.2 实现 `--resume` 命令行参数

**验收标准**：
- [x] 添加 `--resume` 或 `-r` CLI 参数
- [x] 参数接受会话ID作为参数值
- [x] 加载指定会话的消息历史
- [x] 注入历史消息到聊天上下文
- [ ] 测试：使用 `--resume` 恢复会话，验证历史消息加载

- [x] 4.4.3 更新 README.md 使用说明

**验收标准**：
- [x] 在 Chat 模式部分添加 `--resume` 参数说明
- [x] 添加使用示例
- [x] 说明恢复会话的行为（查看历史、继续对话）

### 4.4 AI 知识注入

- [x] 4.4.1 修改 `BuildQueryMessages()` 注入知识

**验收标准**：
- [x] 调用 `memoryManager.GetKnowledgeForPrompt()`
- [x] 将知识添加到系统提示的"记忆与上下文"章节
- [x] 控制知识长度（< 2000 tokens）
- [x] 测试：有知识时验证 Prompt 包含知识

## 5. 命令实现（第 4 周）

### 5.1 Remember 命令

- [x] 5.1.1 实现 `RememberCommand` 结构体

**验收标准**：
- [x] 实现 `Name()` 返回 "remember"
- [x] 实现 `Description()` 返回帮助文本
- [x] 实现 `Run()` 返回 Selector（选择主页选项）
- [x] 实现 `ToSelect()` 返回选择器配置
- [ ] 测试：输入 `/remember`，进入选择器

## 6. 样式定义（第 4 周）

### 6.1 记忆样式

- [ ] 6.1.1 添加记忆相关样式到 `pkg/tui/style/`

**验收标准**：
- [ ] `MemorySelected` - 金色背景、黑色文字
- [ ] `MemoryNormal` - 白色文字
- [ ] `Scroll` - 灰色滚动指示器
- [ ] `SearchHighlight` - 红色背景搜索高亮
- [ ] 测试：在 TUI 中验证样式正确

## 7. 错误处理和日志（贯穿全程）

### 7.1 存储错误处理

- [ ] 7.1.1 实现存储操作的错误处理

> 📚 **知识上下文**：错误处理策略（pattern 003）- 分类错误处理

**验收标准**：
- [ ] 文件写入失败时重试 2-3 次
- [ ] 文件不存在时返回友好错误
- [ ] 权限错误时提示用户
- [ ] 所有错误记录到日志
- [ ] 测试：模拟各种错误场景

### 7.2 日志记录

- [ ] 7.2.1 添加记忆系统日志

**验收标准**：
- [ ] 会话创建/保存时记录 INFO
- [ ] 知识提取时记录 INFO
- [ ] 错误时记录 ERROR
- [ ] 敏感操作时记录 WARN
- [ ] 测试：查看日志验证输出

## 8. 测试（贯穿全程）

### 8.1 单元测试

- [x] 8.1.1 存储层单元测试

**验收标准**：
- [x] `store_test.go` - 测试所有 Store 方法
- [x] 测试索引构建和加载
- [x] 测试搜索功能
- [x] 测试敏感信息过滤
- [ ] 测试覆盖率 > 80%

- [ ] 8.1.2 管理器单元测试

**验收标准**：
- [ ] `manager_test.go` - 测试 Manager 方法
- [ ] 测试单例模式
- [ ] 测试知识缓存
- [ ] 测试覆盖率 > 80%

### 8.2 集成测试

- [ ] 8.2.1 端到端测试

**验收标准**：
- [ ] 创建完整会话并保存
- [ ] 提取知识并验证
- [ ] 搜索会话和知识
- [ ] TUI 导航所有视图
- [ ] AI 知识注入验证

## 9. 文档（第 4 周）

### 9.1 用户文档

- [x] 9.1.1 更新 README.md

**验收标准**：
- [x] 添加记忆系统功能说明
- [x] 添加 `/remember` 命令说明
- [x] 添加隐私政策说明

- [x] 9.1.2 创建使用指南

**验收标准**：
- [x] 如何查看历史会话
- [x] 如何使用知识库
- [x] 如何搜索记忆
- [x] 如何禁用记忆系统

### 9.2 开发文档

- [ ] 9.2.1 添加代码注释

**验收标准**：
- [ ] 所有公共方法添加注释
- [ ] 复杂逻辑添加说明
- [ ] 导出的包添加文档

## 10. 性能优化（可选）

### 10.1 索引优化

- [ ] 10.1.1 实现增量索引更新

**验收标准**：
- [ ] 只在会话保存时更新索引
- [ ] 不在启动时重建索引
- [ ] 测试：1000 个会话，启动 < 200ms

### 10.2 缓存优化

- [ ] 10.2.2 实现知识缓存失效

**验收标准**：
- [ ] 新知识提取后刷新缓存
- [ ] 提供 `/remember reload` 命令手动刷新
- [ ] 测试：提取知识后验证缓存更新

## 基于知识的测试

### 问题 #001 回归测试：异步任务不阻塞

> 📚 **来源**：问题 001 - AI 助手不应自动执行 git commit

**历史问题**：
自动化操作未经用户确认可能导致意外。

**测试场景**：
1. 启动聊天会话
2. 发送多条消息
3. 按 Ctrl+C 退出
4. 记录退出时间

**预期行为**：
- 退出立即返回（< 100ms）
- 不等待知识提取完成
- 后台任务继续执行
- 验证会话文件已保存
- 验证知识文件在 5 秒内生成

### 问题 #003 回归测试：敏感信息脱敏

> 📚 **来源**：问题 003 - 不安全的配置

**历史问题**：
敏感信息可能被记录导致泄露。

**测试场景**：
1. 发送包含 API Key 的消息："我的 API Key 是 sk-abc123def456..."
2. 发送包含密码的消息："密码是: mypassword123"
3. 退出会话
4. 查看保存的会话 JSON 文件

**预期行为**：
- API Key 被替换为 `***REDACTED***`
- 密码被替换为 `***REDACTED***`
- 原始敏感信息不存在于文件中
- 知识提取中也不包含敏感信息

### 问题 #004 回归测试：搜索性能

> 📚 **来源**：问题 004 - 浏览器超时配置

**历史问题**：
随着数据量增加，操作响应时间变长。

**测试场景**：
1. 创建 1000 个模拟会话
2. 执行搜索操作
3. 记录搜索响应时间

**预期行为**：
- 搜索响应时间 < 500ms
- 使用内存索引，不扫描文件
- 分页加载不卡顿
- 启动时间 < 200ms（加载索引）

### 模式 #004 回归测试：单例模式

> 📚 **来源**：模式 004 - 浏览器管理器单例模式

**推荐模式**：
使用单例模式管理全局资源。

**测试场景**：
1. 多次调用 `memory.GetManager()`
2. 验证返回的是同一实例
3. 并发调用 `GetManager()`

**预期行为**：
- 所有调用返回同一实例
- `sync.Once` 确保只初始化一次
- 并发调用安全，无数据竞争
- 内存中只有一个 Manager 实例

---

## 实现进度总结

### 已完成的核心模块

**1. 数据模型** (100%)
- `pkg/model/memory.go` - 所有数据结构定义

**2. 存储层** (100%)
- `pkg/logic/memory/store.go` - 存储接口
- `pkg/logic/memory/filestore.go` - 文件存储实现
- 会话存储、知识存储、索引、搜索、短期记忆、统计

**3. 记忆管理器** (100%)
- `pkg/logic/memory/manager.go` - 单例管理器
- 会话初始化、消息记录、会话结束处理

**4. AI 知识提取** (100%)
- `pkg/logic/memory/extraction.go` - AI 提取和降级策略
- 知识提取 Prompt、LLM 调用、JSON 解析

**5. 敏感信息过滤** (100%)
- `pkg/logic/memory/sensitive.go` - API Key、密码等敏感信息脱敏

**6. Chat 集成** (100%)
- `pkg/logic/memory/chat_integration.go` - Chat 与记忆系统集成
- 初始化、消息记录、退出处理、知识注入

**7. 命令系统** (100%)
- `pkg/logic/memory/remember_command.go` - `/remember` 命令注册

**8. TUI 视图** (60%)
- `pkg/tui/memory/home.go` - 主页和会话列表视图

### 待完善功能

1. 更多 TUI 视图组件（会话详情、知识列表、搜索视图）
2. 单元测试
3. 文档完善
4. 集成测试

### 可立即使用的功能

用户现在可以：
- ✅ 启动 MSA 时自动初始化记忆系统
- ✅ 聊天消息自动记录到短期记忆
- ✅ 退出时自动保存会话并提取知识
- ✅ 使用 `/remember` 命令进入记忆浏览器
- ✅ 查看历史会话列表
- ✅ 知识自动注入到 AI Prompt

### 启动验证

```bash
# 编译项目
go build ./...

# 运行 MSA
./msa

# 在聊天中使用 /remember 命令
/remember
```

---
