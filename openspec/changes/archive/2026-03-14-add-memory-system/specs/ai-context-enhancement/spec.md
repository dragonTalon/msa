# 规格：ai-context-enhancement

> 📚 **知识上下文**：基于模式 002（AI 助手工作流程）- 明确 AI 的职责边界

## ADDED Requirements

### 需求：启动时加载用户知识

系统必须在启动时加载用户的聚合知识，用于增强 AI 回复。

#### 场景：首次启动加载知识

- **当** MSA 启动且记忆系统已初始化
- **那么** 从 `~/.msa/remember/knowledge/` 加载所有知识
- **并且** 聚合知识到内存缓存
- **并且** 加载应在 200ms 内完成（使用索引）
- **并且** 如果加载失败，记录警告但不阻止启动

#### 场景：知识缓存更新

- **当** 会话结束提取出新知识
- **那么** 异步更新内存缓存
- **并且** 下次聊天时使用更新的知识
- **并且** 缓存更新不影响当前会话

### 需求：知识注入到系统提示

系统必须将用户知识注入到 AI 的系统提示中。

#### 场景：构建增强的系统提示

- **当** 调用 AI 生成回复时
- **那么** 在系统提示中添加"记忆与上下文"章节
- **并且** 包含以下知识：
  - 用户画像（投资风格、风险偏好、经验水平）
  - 关注列表（用户关注的股票和板块）
  - 学过的概念（技术指标、术语）
  - 常见问答（用户常问的问题）
- **并且** 格式化为结构化的 Markdown
- **并且** 控制知识长度（不超过 2000 tokens）

#### 场景：相关知识优先注入

- **当** 用户询问特定股票或概念时
- **那么** 优先注入相关知识
- **并且** 突出显示相关的历史讨论
- **并且** AI 可以引用之前的对话

> 📚 **推荐模式**：遵循 AI 助手工作流程（pattern 002）
> 知识注入仅用于增强 AI 回复，不应改变 AI 的核心行为或决策逻辑

### 需求：知识相关性评分

系统应评估知识的相关性，避免注入无关信息。

#### 场景：关键词匹配

- **当** 用户输入包含特定关键词
- **那么** 计算知识的相关性分数：
  - 股票代码匹配：+10 分
  - 概念名称匹配：+10 分
  - 投资风格匹配：+5 分
  - 板块匹配：+5 分
- **并且** 只注入相关性分数 > 5 的知识

#### 场景：上下文窗口限制

- **当** 知识内容超过上下文窗口限制
- **那么** 按相关性分数排序
- **并且** 只保留最相关的知识
- **并且** 记录被舍弃的知识到日志

### 需求：知识来源标注

AI 引用知识时必须标注来源，便于用户验证。

#### 场景：AI 引用用户偏好

- **当** AI 基于用户画像提供建议
- **那么** 可以说"根据你的投资风格偏好..."
- **并且** 可以引用之前学过的概念"正如我们之前讨论的..."
- **并且** 不应编造不存在的知识

#### 场景：AI 引用历史对话

- **当** AI 引用历史对话内容
- **那么** 可以说"你在上次问到..."
- **并且** 可以提供查看历史详情的提示
- **并且** 应准确引用，不歪曲原意

### 需求：知识失效检测

系统应检测知识是否仍然有效，避免过期信息误导。

#### 场景：时间敏感的知识

- **当** 注入时间敏感的知识（如股价数据）
- **那么** 标注知识的时间戳
- **并且** 如果知识超过 7 天，添加"此信息可能已过时"提示
- **并且** AI 应建议用户查询最新数据

#### 场景：用户偏好变化

- **当** 检测到用户偏好变化（如从保守转向激进）
- **那么** 创建新的用户画像知识
- **并且** AI 应使用最新的偏好
- **并且** 可以提及"我注意到你的投资风格有所变化..."

### 需求：知识隐私保护

注入的知识不应包含敏感或隐私信息。

#### 场景：过滤敏感知识

- **当** 准备注入知识时
- **那么** 检查知识是否包含敏感信息
- **并且** 如果包含脱敏标记（***REDACTED***），跳过该知识
- **并且** 记录被过滤的知识

#### 场景：用户控制知识共享

- **当** 用户不想使用知识增强功能
- **那么** 提供 `/remember disable-context` 命令
- **并且** AI 回复不包含知识注入
- **并且** 设置应持久化

### 需求：知识效果反馈

系统应跟踪知识增强的效果，持续优化。

#### 场景：记录知识使用

- **当** AI 引用某条知识
- **那么** 记录使用次数
- **并且** 记录使用时间
- **并且** 用于分析哪些知识最有价值

#### 场景：用户反馈

- **当** 用户对 AI 回复提供反馈（如 👍/👎）
- **那么** 关联到使用的知识
- **并且** 如果负面反馈，考虑降低该知识的权重
- **并且** 如果正面反馈，提高该知识的优先级

> 📚 **历史问题参考**：基于问题 001（AI 助手不应自动执行操作）
> 知识增强仅用于改善 AI 回复质量，不应触发任何自动化操作

### 需求：知识提示控制

系统应允许用户控制知识注入的级别。

#### 场景：简洁模式

- **当** 用户偏好简洁回复
- **那么** 最小化知识注入
- **并且** 只注入最相关的知识
- **并且** AI 回复更简短

#### 场景：详细模式

- **当** 用户偏好详细解释
- **那么** 注入更多相关知识和上下文
- **并且** AI 提供更丰富的分析
- **并且** 可以引用更多历史对话

#### 场景：用户禁用特定知识类型

- **当** 用户不想让 AI 某类知识（如"不要引用我的亏损记录"）
- **那么** 标记该知识类型为禁用
- **并且** AI 不再注入该类知识
- **并且** 设置应持久化
