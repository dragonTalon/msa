# 实现任务：add-cli-chat

## ⚠️ 风险警报

基于历史问题识别的风险：

### 中等：流式通道阻塞

- **相关问题**：知识库问题 #004 (chromedp 超时)
- **缓解措施**：使用 `for-range` 遍历 channel，确保 channel 关闭时正常退出

### 中等：错误信息不清晰

- **相关问题**：知识库模式 error-handling-strategy
- **缓解措施**：所有错误信息包含具体原因和建议操作

---

## 1. 命令参数定义

- [x] 1.1 在 `cmd/root.go` 添加 `-q/--question` 参数
> 📚 **知识上下文**：遵循配置优先级 CLI > ENV > File > Default

- [x] 1.2 在 `cmd/root.go` 添加 `-m/--model` 参数

- [x] 1.3 实现参数互斥逻辑：`-q` 与 `--resume` 同时使用时输出警告

---

## 2. CLI 流式输出模块

- [x] 2.1 创建 `pkg/extcli/` 目录

- [x] 2.2 创建 `pkg/extcli/chat.go`，实现 `Run(ctx, question, model)` 函数

- [x] 2.3 实现配置验证逻辑
> 📚 **知识上下文**：基于模式 error-handling-strategy
> - 参数错误：空问题立即返回
> - 永久错误：配置缺失不重试

- [x] 2.4 实现模型覆盖逻辑
> 📚 **知识上下文**：基于模式 config-auto-fill-on-provider-change
> - `-m` 参数临时覆盖 Model 配置
> - 不修改配置文件

- [x] 2.5 实现流式输出订阅，注册 `StreamOutputManager`

- [x] 2.6 实现三种消息类型的输出格式
- 正文消息：直接输出
- 思考消息：添加 "🤔 思考中..." 前缀
- 工具消息：添加 "🔧 调用工具:" 前缀

- [x] 2.7 实现流式输出完成处理，程序退出码 0

- [x] 2.8 实现错误处理，输出到 stderr，退出码 1

---

## 3. 路由逻辑集成

- [x] 3.1 修改 `cmd/root.go` 的 `runRoot` 函数

- [x] 3.2 添加条件分支：`-q` 参数存在时调用 `cli.Run()`

- [x] 3.3 保持无 `-q` 参数时的 TUI 启动逻辑不变

---

## 4. 验证与测试

- [x] 4.1 编译验证：`go vet ./...`

- [x] 4.2 手动测试：`msa -q "测试问题"`

- [x] 4.3 手动测试：`msa -q "测试问题" -m "deepseek-r1"`

- [x] 4.4 手动测试：空问题返回错误码 1

- [ ] 4.5 手动测试：无 API Key 配置时返回清晰错误

- [x] 4.6 手动测试：`msa -q "test" --resume "xxx"` 输出警告

---

## 基于知识的测试

### 问题 #004 回归测试 (流式通道)

> 📚 **来源**：知识库问题 #004 - 浏览器超时时间过短

**历史问题**：
硬编码超时或阻塞通道操作导致程序无响应。

**测试场景**：
1. 执行 `msa -q "问题"`
2. 等待流式输出完成
3. 验证程序正常退出
4. 验证无阻塞、无死锁

**预期行为**：
- 流式输出正常显示
- 程序退出码为 0
- 无 goroutine 泄漏

### 错误处理验证

> 📚 **来源**：知识库模式 error-handling-strategy

**测试场景**：
1. 空问题：`msa -q ""`
2. 无配置：删除配置文件后执行
3. 无模型：配置文件中 Model 为空，不指定 `-m`

**预期行为**：
- 错误信息输出到 stderr
- 包含具体原因和建议操作
- 退出码为 1